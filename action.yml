name: 'GitVersioning'
description: 'runs nbgv and saves the output to nbgv.json'
author: 'ctaggart'
inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'node12'
  main: 'lib/main.js'
